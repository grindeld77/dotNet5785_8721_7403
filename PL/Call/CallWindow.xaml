<Window x:Class="PL.Call.CallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL.Call"
        Title="Call Details" Height="600" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <StackPanel Margin="20" HorizontalAlignment="Center">
            <!-- header -->
            <TextBlock Text="Call Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />

            <!-- id -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Call ID:" Width="100" VerticalAlignment="Center" />
                <TextBox Text="{Binding CurrentCall.Id}" IsReadOnly="True" Width="200" />
            </StackPanel>

            <!-- call Description -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Description:" Width="100" VerticalAlignment="Center" />
                <TextBox Text="{Binding CurrentCall.Description}" Width="200" />
            </StackPanel>

            <!-- call type -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Type:" Width="100" VerticalAlignment="Center" />
                <ComboBox ItemsSource="{Binding Source={StaticResource OpenCallCollectionTypeKey}}"
                          SelectedItem="{Binding CurrentCall.Type}"
                          Width="200" />
            </StackPanel>

            <!-- call address -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Full Address:" Width="100" VerticalAlignment="Center" />
                <TextBox Text="{Binding CurrentCall.FullAddress}" Width="200" />
            </StackPanel>

            <!-- open time -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Open Time:" Width="100" VerticalAlignment="Center" />
                <TextBox Text="{Binding CurrentCall.OpenTime}" IsReadOnly="True" Width="200" />
            </StackPanel>

            <!-- max end time -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Max End Time:" Width="100" VerticalAlignment="Center" />
                <TextBox Text="{Binding CurrentCall.MaxEndTime}" Width="200" />
            </StackPanel>

            <!-- call status -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Status:" Width="100" VerticalAlignment="Center" />
                <TextBox Text="{Binding CurrentCall.Status}" IsReadOnly="True" Width="200" />
            </StackPanel>

            <!-- assignmentList -->
            <GroupBox Header="Assignments" Margin="0,20" Height="123">
                <DataGrid ItemsSource="{Binding CurrentCall.Assignments}" AutoGenerateColumns="False" CanUserAddRows="False" Height="123">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Volunteer ID" Binding="{Binding VolunteerId}" />
                        <DataGridTextColumn Header="Volunteer Name" Binding="{Binding VolunteerName}" />
                        <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime}" />
                        <DataGridTextColumn Header="End Time" Binding="{Binding EndTime}" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- buttons area -->
            <StackPanel Orientation="Horizontal" Margin="0,20" HorizontalAlignment="Center">
                <Button Content="{Binding ButtonText}" Width="100" Margin="10" Click="SaveButton_Click" />
                <Button Content="Cancel" Width="100" Margin="10" Click="Cancel_Click" />
                <Button Content="Delete" Width="100" Margin="10" Click="DeleteButton_Click" Visibility="{Binding IsDeleteButtonVisible}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>